# @package _global_
generation:
  type: adaption
  prompt_template: "You are a helpful assistant. Generate a detailed response for: {text}"
  num_samples: 100
  output_format: jsonl
  deduplicate: true
  save_intermediate: true
  adaption_api:
    api_key: ${oc.env:ADAPTION_API_KEY,}
